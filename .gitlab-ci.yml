stages:
  - build
  - test

build-amd64:
  stage: build
  before_script:
    - apt-get update -qq && apt-get install -y -qq build-essential git
  script:
    - make -j4
  tags:
    - linux
    - docker

test-arm64:
  stage: test
  before_script:
    - apt-get update -qq && apt-get install -y -qq build-essential git
  script:
    - make test -j4
    - ./testSuite
  tags:
    - linux
    - docker
